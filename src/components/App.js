const React = require( 'react' );

class App extends React.Component {
    constructor ( props ) {
        super( props );
        this.state = {};
    }

    render () {
        return (
            <p>Nothing to display yet. Dumb dummy dumbstruck !</p>
        );
    }
}

export default App;
