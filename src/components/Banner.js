const React = require( 'react' );

function Banner ( props ) {
    return (
        <div className="branding">
            <h1 className="site-title">Internulejo</h1>
            <p>Find a web company near you&nbsp;!</p>
        </div>
    );
}

export default Banner;
